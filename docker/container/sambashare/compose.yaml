services:
  sambashare:
    image: dockurr/samba
    container_name: sambashare
    environment:
      - NAME=sambashare
      - USER=homelab
      - PASS=admin132109bali
      - UID=1000
      - GID=1000
    ports:
      - 445:445
    volumes:
      - /DATA/SHARE:/storage
    restart: unless-stopped
