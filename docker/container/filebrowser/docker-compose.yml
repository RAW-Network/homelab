services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    volumes:
      - /DATA:/srv
      - ./config:/config
      - ./database:/database
    ports:
      - 8095:80
    restart: unless-stopped
